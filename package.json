{
  "name": "kutubkhana",
  "version": "1.0.0",
  "private": true,
  "description": "مكتبة المصباح - نظام إدارة المكتبة",
  "scripts": {
    "build": "node scripts/build-config.js",
    "build:android": "node scripts/build-android.js",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android": "npm run build:android && npm run cap:sync && npm run cap:open",
    "apk": "node scripts/build-apk.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}